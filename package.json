{
  "name": "attendance-system",
  "version": "0.1.0",
  "private": true,
  "main": "apps/backend/dist/server.js",
  "scripts": {
    "predev": "npm run predev --prefix apps/backend",
    "dev": "npm run dev:raw --prefix apps/backend",
    "backend:dev": "npm run dev --prefix apps/backend",
    "backend:typecheck": "npm run typecheck --prefix apps/backend",
    "backend:build": "npm run build --prefix apps/backend",
    "backend:test": "npm run test --prefix apps/backend",
    "backend:lint": "npm run lint --prefix apps/backend",
    "frontend:dev": "npm run dev --prefix apps/frontend",
    "frontend:typecheck": "npm run typecheck --prefix apps/frontend",
    "frontend:build": "npm run build --prefix apps/frontend",
    "build": "npm run build --prefix apps/backend",
    "lint": "npm run lint --prefix apps/backend",
    "test": "npm run test --prefix apps/backend",
    "typecheck": "npm run typecheck --prefix apps/backend",
    "start": "npm run start --prefix apps/backend",
    "db:generate": "npm run db:generate --prefix apps/backend",
    "db:migrate": "npm run db:migrate --prefix apps/backend",
    "db:diff": "npm run db:diff --prefix apps/backend",
    "db:deploy": "npm run db:deploy --prefix apps/backend",
    "db:seed": "npm run db:seed --prefix apps/backend",
    "db:seed:reset": "npm run db:seed:reset --prefix apps/backend",
    "db:studio": "npm run db:studio --prefix apps/backend",
    "db:seed:prod": "npm run db:seed:prod --prefix apps/backend",
    "setup:mtx": "npm run setup:mtx --prefix apps/backend",
    "excel:extract-images": "npm run excel:extract-images --prefix apps/backend"
  },
  "prisma": {
    "seed": "npm run db:seed --prefix apps/backend"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^7.0.0",
    "@fastify/multipart": "^7.7.0",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/static": "^7.0.4",
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.0.0",
    "exceljs": "^4.3.0",
    "fastify": "^4.20.0",
    "ioredis": "^5.3.2",
    "jszip": "^3.10.1",
    "node-cron": "^3.0.2",
    "onvif": "^0.8.1",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "eslint": "^8.51.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "vitest": "^1.6.1"
  }
}
